<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/chats" method="post">
    <input type="text" placeholder="enter sender name" name="from"><br><br>
    <textarea name="msg" id="" placeholder="enter your message"></textarea><br><br>
    <input type="text" name="to" id="" placeholder="enter reciever name"><br><br>
    <button>Create Chat</button>
</form>

<style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      color: #333;
    }
    input{
        width: 100%;
      height: 59px;
      padding: 20px;
      border: 2px solid #e0e0e0;
      border-radius: 12px;
      font-family: inherit;
      font-size: 1.1rem;
      line-height: 1.6;
      resize: vertical;
      transition: all 0.3s ease;
      background: #fafafa;
      color: #333;
      margin-bottom: 20px;
    }
    input:focus {
      outline: none;
      border-color: #667eea;
      background: #fff;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
      transform: translateY(-2px);
    }

    input::placeholder {
      color: #999;
      font-style: italic;
    }

    h3 {
      color: white;
      font-size: 2.5rem;
      font-weight: 300;
      text-align: center;
      margin-bottom: 30px;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
    }

    form {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
      border-left: 4px solid #667eea;
      animation: fadeInScale 0.5s ease forwards;
    }

    textarea {
      width: 100%;
      min-height: 150px;
      padding: 20px;
      border: 2px solid #e0e0e0;
      border-radius: 12px;
      font-family: inherit;
      font-size: 1.1rem;
      line-height: 1.6;
      resize: vertical;
      transition: all 0.3s ease;
      background: #fafafa;
      color: #333;
      margin-bottom: 20px;
    }

    textarea:focus {
      outline: none;
      border-color: #667eea;
      background: #fff;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
      transform: translateY(-2px);
    }

    textarea::placeholder {
      color: #999;
      font-style: italic;
    }

    button {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 15px 30px;
      border-radius: 25px;
      cursor: pointer;
      font-size: 1.1rem;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
      min-width: 120px;
      display: block;
      margin: 0 auto;
    }

    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }

    button:active {
      transform: translateY(-1px);
    }

    br {
      display: none;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      body {
        padding: 15px;
      }

      h3 {
        font-size: 2rem;
      }

      form {
        padding: 25px;
        margin: 10px;
      }

      textarea {
        min-height: 120px;
        padding: 15px;
        font-size: 1rem;
      }

      button {
        width: 100%;
        max-width: 200px;
      }
    }

    /* Animation */
    @keyframes fadeInScale {
      from {
        opacity: 0;
        transform: scale(0.9);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
  </style>
</body>
</html>